#include "student.kalalev18.h"
char* STUDENT::getFullName()
{
	char* result = new char[sizeof(firstName) + sizeof(lastName) + sizeof(char)];
	//TODO: copy fullName+ ' ' + lastName
	return firstName;
}